#include<iostream>
using namespace std;
int palindrome(int n)
{
int rev = 0;
int dup = n;
while(n>0)
{
int lastdigit = n % 10;
rev= (rev*10)+lastdigit;
n = n/10;
}
if(rev == dup)
{
cout<<"palindrome number";
}
else
{
cout<<"not palindrome number";
}
}
int main()
{
int t;
cin>>t;
palindrome(t);
}
