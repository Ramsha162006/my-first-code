#include<iostream>
using namespace std;
int armstron(int n)
{
int sum = 0;
int dup = n;
while(n>0)
{
int r = n%10;
sum = sum + r*r*r;
n=n/10;
}
if(sum == dup)
{ cout<<"armstrong number";
}
else
{
cout<<"not armstrong number";
}
}
int main()
int t;
cin>>t;
armstrong(t);
}
